<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PostMania</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">

    <link rel="stylesheet" href="/stylesheets/feed.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="feed w-full min-h-screen bg-zinc-900 p-5 text-white">
        <div class="flex justify-end p-4 z-70 w-[94%] bg-zinc-900 fixed items-center gap-3 top-0">
            <h3><a href="/profile" class="text-white">
                    <div class="w-10 h-10 rounded-[50%] border-[1px] border-zinc-700 overflow-hidden">
                            <img class="w-full h-full object-cover" src="<%= profile.profilepic %>" onerror="this.src='./images/uploads/default.jpg'">
                    </div>
            </h3>
            <button class="border-2 border-blue-500 rounded-md px-2 py-1 text-blue-500"><a
                    href="/logout">Logout</a></button>
        </div>
        <h5 class="header my-5">All Posts</h5>
        <div class="postcontainer mt-5">
            <% if(posts.length>0){%>
                <%posts.reverse().forEach((post)=>{%>
                    <div class="post w-1/3 p-4 mb-4 rounded-md bg-zinc-800 border-[1px] border-zinc-700">
                        <div class="flex gap-2 items-center mb-3">
                            <div class="w-10 h-10 rounded-[50%] border-[1px] border-zinc-700 overflow-hidden">
                                <img class="w-full h-full object-cover" src="<%=post.user.profilepic %>" onerror="this.src='./images/uploads/default.jpg'"
                                    alt="img">
                            </div>
                            <div class=" flex flex-col gap-0">
                                <h4 class="text-blue-500 curser-pointer"><a href="/user/<%= post.user.username%>">@<%= post.user.username%></a>
                                </h4>
                                <p class=" text-sm text-zinc-500"><%= formatDistanceToNow(new Date(post.date), { addSuffix: true }) %></p>
                            </div>
                        </div>

                        <p class="text-sm tracking-tight">
                            <%= post.content %>
                        </p>
                        <div class="mt-5 text-sm">
                            <%= post.likes.length%> Likes
                        </div>
                        <div class="btns gap-4 flex">
                            <a href="/like/<%= post._id%>" class="text-blue-500">
                                <%= post.likes.indexOf(profile._id) === -1 ? "Like" : "Unlike" %>
                            </a>
                            <a href="/edit/<%= post._id%>" class="text-zinc-600">
                                <%if(post.user.email==profile.email){%>
                                    edit
                                    <%}%>
                            </a>
                        </div>
                    </div>

                    <%})%>
                        <%}else{%>
                            <h3 class="text-white text-center text-zinc-500">No Posts Yet.</h3>
                            <%}%>
        </div>
    </div>
    <script src="/javascripts/feed.js"></script>
</body>

</html>