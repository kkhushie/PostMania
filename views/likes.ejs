<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Likes</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <link rel="stylesheet" href="/stylesheets/profile.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="container w-full min-h-screen bg-zinc-900 p-5 text-white">
        <h1 class="mb-5 text-3xl">Users who liked the post</h1>
        <div class="bg-zinc-800 w-full sm:w-3/4 md:w-1/2 px-4  rounded-lg">
            <% if (likes && likes.length> 0) { %>
                <% likes.forEach((user)=> { %>
                    <div class="user border-b-[1px] border-b-zinc-700 flex gap-3 items-center py-3 justify-between">
                        <div class="flex gap-3 items-center">

                            <div class="w-10 h-10 flex rounded-full border-[1px] border-zinc-700 overflow-hidden">
                                <img class="w-full h-full object-cover" src="<%= user.profilepic %>"
                                    onerror="this.src='/images/uploads/default.jpg'" alt="User profile picture">
                            </div>
                            <div>
                                <h4 class="text-white capitalize"><a href="/user/<%= user.username %>">
                                        <%= user.name %>
                                    </a></h4>
                                <p class="text-blue-500 lowercase">@<%= user.username %>
                                </p>
                            </div>
                            
                        </div>
                        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18"
                                                viewBox="0 0 24 24" fill="crimson">
                                                <path
                                                    d="M12 4.435c-1.989-5.399-12-4.597-12 3.568 0 4.068 3.06 9.481 12 14.997 8.94-5.516 12-10.929 12-14.997 0-8.118-10-8.999-12-3.568z" />
                                            </svg>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <p>No likes yet.</p>
                            <% } %>
        </div>
    </div>
</body>

</html>