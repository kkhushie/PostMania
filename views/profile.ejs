<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 p-5 text-white">
        <div class="flex justify-end items-center w-[94%] fixed gap-3">
            <button><a href="/feed">explore</a></button>
            <button class="border-2 border-blue-500 rounded-md px-2 py-1 text-blue-500"><a
                    href="/logout">Logout</a></button>
        </div>
        <div class="mt-10">
            <div class="flex gap-3">
                <div class="w-10 h-10 rounded-[50%] border-[1px] border-zinc-700 overflow-hidden">
                    <a href="/profile/upload">
                        <img class="w-full h-full object-cover" src="/images/uploads/<%=user.profilepic %>" alt="img">
                    </a>
                </div>
                <h1 class="text-3xl font-semibold"><span class="font-light">Hello</span>, <%=user.name%>👋</h1>
            </div>
                <h5 class="my-5">You can create a new post.</h5>
        <form action="/post" method="post">
            <textarea name="content" placeholder="What's on your mind?"
                class=" w-1/3 p-3 resize-none bg-transparent outline-none border-2 rounded-md border-zinc-800 block"></textarea>
            <input class="px-3 w-40 py-1 bg-blue-500 block rounded-md mt-2" value="Create New Post" type="submit">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().map((post)=>{ %>
                    <div class="post w-1/3 p-4 mb-4 rounded-md bg-zinc-800 border-[1px] border-zinc-700">
                        <h4 class="text-blue-500 mb-2">@<%= user.username%>
                        </h4>
                        <p class="text-sm tracking-tight">
                            <%= post.content %>
                        </p>
                        <div class="mt-5 text-sm">
                            <%= post.likes.length%> Likes
                        </div>

                        <div class="btns gap-4 mt-1 flex">
                            <a href="/like/<%= post._id%>" class="text-blue-500">
                                <%= (post.likes.indexOf(user._id)==-1)?"Like":"Unlike" %>
                            </a> <a href="/edit/<%= post._id%>" class="text-zinc-600">edit</a>
                        </div>
                    </div>
                    <%})%>
            </div>
        </div>
        
    </div>
    </div>
</body>

</html>